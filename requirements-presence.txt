# Requirements for Presence Module
# Install with: pip install -r requirements-presence.txt

# Web framework
flask>=3.1.0

# Serial communication (for fingerprint sensor)
pyserial>=3.5

# RFID reader (only works on Raspberry Pi)
# mfrc522>=0.0.7  # Not available on Windows

# Facial recognition (only works on Raspberry Pi)
# face-recognition>=1.3.0  # Not available on Windows
# picamera2>=0.3.0  # Not available on Windows

# Testing
pytest>=7.0.0

# Note: Hardware-specific libraries (mfrc522, face-recognition, picamera2)
# are only needed when deploying on Raspberry Pi.
# In development mode (Windows), manual input is used for testing.